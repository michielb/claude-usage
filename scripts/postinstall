#!/bin/bash
# Launch ClaudeUsage after installation, as the installing user (not root)
LOGGED_IN_USER=$(stat -f "%Su" /dev/console)
if [ -n "$LOGGED_IN_USER" ] && [ "$LOGGED_IN_USER" != "root" ]; then
    sudo -u "$LOGGED_IN_USER" open -a /Applications/ClaudeUsage.app
fi
exit 0
